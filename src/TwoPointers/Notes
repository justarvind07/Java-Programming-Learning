        Pattern: Two Pointers

Q1:- (Arrays_Sort)    Start an array consisting of only 0s and 1s.

Q2:- (Arrays_Sort01) sort an array consisting of only 0s and 1s.

Q3:- (EvenAndOddSort) Given an arrays of integer 'a', move all the even integers at the beginning of the array followed by all the odd integers. The relative order of odd or even integers dies not
matter. Return
any array the satisfies the condition.

Q4:- (Non_Decreasing_Square_Arrays) Given an integer array 'a' sorted in non-decreasing order, return an array of the squares of each number sorted in non-decreasing order.

Q5:-  (PrefixSum)  Given an integer array 'a', return the prefix sum/running sum in the same array without creating a new array.

Q6:-    Given an array of integer of size n. Answer q queries where you need to print the sum of values in a given range of indices from 1 to r(both included)
NOTE:   The value of 1 and r in queries follow 1-based indexing.

Q7:-

Q8:-

